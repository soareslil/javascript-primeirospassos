console.log('trabalhando com listas');

const listasDeDestinos = new Array(
    `Salvador`, 
    `Rio de Janeiro`,
    `Gramado`
);

console.log(listasDeDestinos);

listasDeDestinos.push(`São Paulo`);

console.log(listasDeDestinos);

listasDeDestinos.splice(1,1);
console.log(listasDeDestinos);

console.log(listasDeDestinos[2], listasDeDestinos[1]);


// O que aprendemos nessa aula:

// Criação de Arrays;

// Inserção de itens em um array;

// Array splice;

// Palavras reservadas;

// Indices do array;